# 💻 Networking Commands Cheat Sheet

| Command | Purpose | Example |
|----------|----------|----------|
| ping | Check network connectivity | `ping google.com` |
| traceroute / tracert | Show route packets take | `traceroute google.com` |
| netstat | Display active connections | `netstat -tuln` |
| curl | Make HTTP requests | `curl https://api.github.com` |
| dig | DNS lookup | `dig google.com` |
| nslookup | DNS resolution test | `nslookup github.com` |

### 🧠 Bonus
- `ifconfig` / `ip a` → Show network interfaces  
- `ss -tuln` → Display open ports  
- `route -n` → View routing table  
- `hostname -I` → Display local IP
